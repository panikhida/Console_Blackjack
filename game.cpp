#include "includes.h"
#include "game.h"
void blackjack()
{
    std::vector<Card> deck;
    std::vector<Card> deck_plr;
    std::vector<Card> deck_dlr;
    while (true) {
        int cash { 0 };
        fillDeck(deck);
        shuffleDeck(deck);

    }
}